version: '3.8'

services:
  judgehub-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: judgehub-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
